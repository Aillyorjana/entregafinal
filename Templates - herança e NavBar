#templates/base.html (essencial)

<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>{% block title %}PF Blog{% endblock %}</title>
  <link rel="stylesheet" href="{% static 'css/bootstrap.min.css' %}">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{% url 'pages:list' %}">PF Blog</a>
    <div class="collapse navbar-collapse">
      <ul class="navbar-nav me-auto">
        <li class="nav-item"><a class="nav-link" href="{% url 'pages:list' %}">Páginas</a></li>
        <li class="nav-item"><a class="nav-link" href="{% url 'about' %}">Sobre</a></li>
      </ul>
      <ul class="navbar-nav ms-auto">
        {% if user.is_authenticated %}
          <li class="nav-item"><a class="nav-link" href="{% url 'accounts:profile' %}">Perfil</a></li>
          <li class="nav-item"><a class="nav-link" href="{% url 'messenger:inbox' %}">Mensagens</a></li>
          <li class="nav-item"><a class="nav-link" href="{% url 'accounts:logout' %}">Logout</a></li>
        {% else %}
          <li class="nav-item"><a class="nav-link" href="{% url 'accounts:login' %}">Login</a></li>
          <li class="nav-item"><a class="nav-link" href="{% url 'accounts:register' %}">Inscrever-se</a></li>
        {% endif %}
      </ul>
    </div>
  </nav>

  <div class="container mt-4">
    {% block content %}{% endblock %}
  </div>
</body>
</html>

#pages/pages_list.html

{% extends "base.html" %}
{% block title %}Páginas{% endblock %}
{% block content %}
  <h1>Páginas</h1>
  <form method="get">
    <input type="text" name="q" placeholder="Pesquisar..." value="{{ request.GET.q }}">
    <button>Buscar</button>
  </form>

  {% if pages %}
    <div class="row">
      {% for page in pages %}
        <div class="col-md-4">
          <h3>{{ page.title }}</h3>
          <p>{{ page.subtitle }}</p>
          <p><small>{{ page.published_at }}</small></p>
          <a href="{{ page.get_absolute_url }}">Leia mais</a>
        </div>
      {% endfor %}
    </div>
    {% if is_paginated %}
      <!-- navegação de páginas -->
    {% endif %}
  {% else %}
    <p>Ainda não há páginas.</p>
  {% endif %}
{% endblock %}
